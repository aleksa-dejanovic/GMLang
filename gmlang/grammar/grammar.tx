import graph_definition
import graph_context
import expression

File:
    commands*=Command
;

Command[ws='\t \r']:
    '\n'* (
    GraphDefinitionCommand
    | GraphContextCommand
    | LetStatement
    ) ('\n'+)
;




